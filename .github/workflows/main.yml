# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  push:
    branches: [main]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      - name: Logseq Publish 🚩
        uses: logseq/publish-spa@v0.3.0
        # with:
        #   graph-directory: DevOpsNotes

      - name: add a nojekyll file
        run: touch $GITHUB_WORKSPACE/www/.nojekyll

      - name: Add SEO Meta Tags
        run: |
          echo '<meta name="description" content="Your site description here">' >> $GITHUB_WORKSPACE/www/index.html
          echo '<meta name="keywords" content="keyword1, keyword2, keyword3">' >> $GITHUB_WORKSPACE/www/index.html

      - name: Generate Sitemap
        uses: simply007/sitemap-generator-action@v1
        with:
          base-url: 'https://pofengliu.github.io/devops-notes'
          output-folder: 'www'
  
      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.4.1
        with:
          branch: gh-pages # The branch the action should deploy to.
          folder: www # The folder the action should deploy.
          clean: true
          single-commit: true
